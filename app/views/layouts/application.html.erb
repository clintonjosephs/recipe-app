<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body>
      <nav class="navbar navbar-expand-lg" style="background-color: var(--accent-color)">
        <div class="container">
          <a class="navbar-brand display-6" href="#">RecipeBook</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="#">Link</a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Dropdown
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li><a class="dropdown-item" href="#">Action</a></li>
                  <li><a class="dropdown-item" href="#">Another action</a></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="#">Something else here</a></li>
                </ul>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled">Disabled</a>
              </li>
            </ul>
            <div class="d-flex flex-column">
              <div class="d-flex flex-row align-items-center">
                  <div>
                    <% if current_user %>
                      <% if current_user.image.present? %>
                        <%= image_tag current_user.image, class: "bio-photo" %>
                      <% else %>
                      <%= image_tag "https://cdn.pixabay.com/photo/2016/04/01/10/11/avatar-1299805__340.png", class: "bio-photo" %>
                      <% end %>
                    <% else %>
                      <%= link_to '<i class="fa fa-sign-in" aria-hidden="true"></i> Sign In'.html_safe, new_user_session_path, :"data-turbolinks" => false, class: "nav-link" %>
                    <% end %>
                  </div>
                  <div class="d-flex flex-column">
                    <span><%= current_user.name if current_user %></span>
                    <span><%= current_user.email if current_user %></span>
                    <span><%= link_to '<i class="fa fa-sign-out" aria-hidden="true"></i>Sign Out'.html_safe, destroy_user_session_path, method: :delete, :"data-turbolinks" => false, class: "nav-link" if current_user %></span>
                  </div>
              </div> 
            </div>
          </div>
        </div>
      </nav>
      <section class="container">
        <% if flash.any? %>
          <% flash.each do |type, msg| %>
            <div class="alert alert-<%= type %> text-center">
              <%= msg.html_safe %>
            </div>
          <% end %>
        <% end %>
        <%= yield %>
    </div>
  </body>
</html>
