<%= button_to "Sign Out", destroy_user_session_path, method: 'delete', :"data-turbolinks" => false %>

<%= image_tag current_user.image.variant(resize_to_fill: [150, nil]), class: "rounded-circle mr-2" if current_user %>